<section class="container page-head"><h1><%= table %></h1></section><section class="container admin-layout"><%- include('nav') %><div class="page-card"><div class="table-scroll"><table class="table"><% if(rows.length){ %><tr><% Object.keys(rows[0]).forEach(k=>{ %><th><%= k %></th><% }) %></tr><% rows.forEach(row=>{ %><tr><% Object.keys(row).forEach(k=>{ %><td><%= row[k] %></td><% }) %></tr><% }) %><% } %></table></div></div></section>
