<section class="container page-head"><h1>Kiểm tra database</h1></section><section class="container admin-layout"><%- include('nav') %><div class="page-card"><table class="table"><% result.forEach(r=>{ %><tr><td><%= r.table %></td><td><%= r.exists ? '✅ Có' : '❌ Thiếu' %></td></tr><% }) %></table><p>Nếu còn bảng thiếu, chạy các file SQL trong thư mục <code>database/</code>.</p></div></section>
